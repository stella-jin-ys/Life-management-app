# Server-only Edge Function environment. Copy to supabase/.env.local; never expose this key through Vite or GitHub Pages.
OPENAI_API_KEY=replace-with-a-server-only-project-key
OPENAI_MODEL=gpt-5.4-mini
